#!/usr/bin/env sh

case "$1" in
    w)
        rofi -show window
        ;;
    d)
        rofi -show drun
        ;;
    u)
        rofi -show emoji
        ;;
    *)
        echo "Select options: w - window | d - drun | u - emoji"
esac
